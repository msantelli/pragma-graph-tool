# Desktop app (Electron main process, build outputs)
electron/
dist-electron/

# CLI package (not part of web app)
cli/

# NOTE: packages/core must NOT be ignored — the web build compiles it from
# source (vite aliases @pragma-graph/core to packages/core/src).

# Research docs
research/
